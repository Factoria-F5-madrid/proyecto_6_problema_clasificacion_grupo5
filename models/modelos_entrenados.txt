aqui estaran los Modelos entrenados (.pkl, .joblib) y metadatos
OJO : esta carpeta es para modelos PKL , no tiene nada que ver con la carpeta Models del backend que sera para los definición de modelos de datos / clases (Modelo) de la BBDD
(este archivo tambien se eliminará es solo para que se suba la carpeta)